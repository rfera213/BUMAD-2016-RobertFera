<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="robertfera.mad.bu.edu.bumad_2016_robertfera">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BUTodayArticleList" />
        <activity android:name=".BUTodayArticleDetail" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".CoursesCollegeList" />
        <activity android:name=".CoursesCollegeSubjectsAndDepartmentsList" />
        <activity android:name=".CoursesList" />
        <activity android:name=".CourseDetail" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB6-ujQrWzej71xjcaxmkl-8Pyh7FgfTD4" />

        <activity
            android:name=".BusMap"
            android:label="@string/title_activity_bus_map" />
        <activity android:name=".DirectorySearch" />
        <activity android:name=".DirectoryDetail" />
        <activity android:name=".CalendarTopics" />
        <activity android:name=".CalendarEvents" />
        <activity android:name=".CalendarEventDetail" />
        <activity android:name=".Map" />
        <activity android:name=".AppInfo"></activity>
    </application>

</manifest>